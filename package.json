{
  "name": "ferit-haber-root",
  "version": "1.0.0",
  "description": "Ferit Tercan Portfolyo & Haber Sitesi",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {},
  "engines": {
    "node": "18.x"
  }
}
